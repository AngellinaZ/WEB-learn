Array	方法	返回值	解释
	"slice(0,1) "	截取Array的部分，返回一个新的Array	从索引0开始，到索引3结束，但不包括索引3
	pop()	返回 pop() 出来的值	空数组继续pop不会报错，而是返回undefined
	push()	返回Array新的长度	
	shift()	返回 shift() 出来的值	空数组继续shift不会报错，而是返回undefined
	unshift()	返回Array新的长度	
	sort()	返回排序后的原数组	
	reverse()	返回到了个个的原数组	
	"arr.splice(2, 3, 'G', 'F')"	返回删除的元素	"从索引2开始删除3个元素,然后再添加两个元素:"
	concat()	返回一个新的数组， 不修改原来的数组	把当前的Array和另一个Array连接起来
	join('-')	返回链接后的字符串	数组的每个元素都用指定的字符串('-')链接
